# ================================================================
#  AMR ROBOT — pip requirements
#  NIA Semi-Humanoid Platform
#  jetson-containers edition | L4T 36.4.7 | JetPack 6.2.1
#
#  INSTALL INSIDE your robot container only:
#    pip install -r requirements.txt
#    pip install git+https://github.com/openai/CLIP.git
#
#  DO NOT pip install these — come from base image / other sources:
#    torch        → dustynv/ultralytics base image (CUDA 12.8 ARM64)
#    torchvision  → dustynv/ultralytics base image
#    opencv       → dustynv/ultralytics base image (CUDA build)
#    faiss        → jetson-containers pip server (ARM64 wheel)
#    open3d       → build from source or skip (no ARM64 pip wheel)
#    CLIP         → pip install git+github.com/openai/CLIP.git
#    pyrealsense2 → apt install librealsense2-dev + pip install
#    rclpy        → apt install ros-humble-rclpy
#    Gemma 3 1B   → dustynv/local_llm Docker container (port 9000)
# ================================================================

# ── Perception ───────────────────────────────────────────────────
ultralytics>=8.3.0
deep-sort-realtime>=1.3.2
Pillow>=10.0.0
ftfy>=6.1.0
regex>=2023.0.0

# ── World Model ──────────────────────────────────────────────────
neo4j>=5.0.0

# ── Planner ──────────────────────────────────────────────────────
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
openai>=1.40.0
sentence-transformers>=3.0.0
pydantic>=2.0.0

# ── Shared ───────────────────────────────────────────────────────
redis>=5.0.0
scipy>=1.11.0
typing-extensions>=4.9.0
